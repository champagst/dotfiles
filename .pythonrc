def exf(f, globals=globals(), locals=locals()):
   with open(f) as fin:
      code = compile(fin.read(), f, 'exec')
      exec(code, globals, locals)
print("\nDefined exf.")

try:
   from see import see
except:
   print("\nCould not import see.")
else:
   print("\nImported see.")
